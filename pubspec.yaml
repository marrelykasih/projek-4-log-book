name: logbook_app_015
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

# BAGIAN PENTING YANG TADI ERROR (JANGAN DIHAPUS)
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.2 
  mongo_dart: ^0.10.8
  flutter_dotenv: ^6.0.0
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # Pendaftaran Gambar Aset
  assets:
    - assets/intro1.png
    - assets/intro2.png
    - assets/intro3.png
    - .env